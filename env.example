# .env.example

# Placeholder if needed for API keys or remote storage
CHROMA_PATH=./chroma_db

export ENABLE_TRACE_ATTRIBUTES=true  # in dev
export ENABLE_TRACE_ATTRIBUTES=false  # in prod

ENV=prod            # or: ENV=prod
LOG_LEVEL=INFO 
DATABASE_URL=postgresql+psycopg2://postgres:yourpass@localhost:5432/llm_db
